// Component
import * as styled from "./styled"

const Button = (props) => {
	// -------------------------------------------------
	// Render
	// -------------------------------------------------

	return (
		<styled.Container {...props} />
	)
}

export default Button